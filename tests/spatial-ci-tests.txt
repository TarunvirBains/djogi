# Cargo test names that must run in the Spatial (PostGIS) CI lane with
# `--all-features`. `cargo xtask check-spatial-ci` fails if a Cargo test that
# contains spatial-gated scenarios or `#[djogi_test(extensions = ["postgis"])]`
# is missing from this manifest.
spatial
spatial_polish
sync_models_live
sync_models_catalog_live
indexes_live
pair_area_overlap_ratio_live
pair_only_annotation_rejection
set_op_sql_shape
row_aggregate_mvt_live
spatial_live
spatial_scalar_live
window_live
bench
