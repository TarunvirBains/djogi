> [Back to README](../../ReadMe.MD) | [All Specs](./index.md)

# Resolved Design Decisions

---

## 19. Resolved Design Decisions

| Decision | Resolution |
|---|---|
| Default PK type | `HeerIdRecencyBiased` / `HeerIdDesc` — `BIGINT DEFAULT heerid_next_desc()`, database-native, time-ordered |
| Default PK ID pattern | `create()` uses `DEFAULT heerid_next_desc()` + `RETURNING id` |
| HeerId ID pattern — bulk | `HeerId::generate_many(n)` pre-allocates IDs before bulk INSERT |
| HeerId ID pattern — form | `HeerId::generate()` at form render time; INSERT with `ON CONFLICT DO NOTHING` |
| Serial PKs | Opt-in via `#[model(pk = Serial)]` |
| Node identity | `HEER_NODE_ID` and `HEER_RANJ_NODE_ID` environment variables; operator provisions `heer_nodes` and starts services with the selected node IDs before connections attempt HeeRanjID operations; Djogi does not read these vars directly — wire them into the pool's `post_connect` hook |
| HeerId JSON serialization | Always string — enforced by HeerId serde impl, no annotation needed |
| Datetime library | `time` crate — cleaner API, no CVE history |
| Framework fields (`id`, `created_at`, `updated_at`) | Macro-injected — real fields, not written by developer |
| `create()` input | Struct itself — no separate `CreateVehicle` wrapper |
| FK loading strategy | Explicit `.fetch()` or `.prefetch()` — no lazy loading |
| M2M strategy | Explicit through models required; `ManyToMany<T>` trait for convenience methods |
| M2M method naming | Explicit `RELATION` const — auto-pluralization is error-prone |
| M2M directionality | Each direction declared separately with its own `impl ManyToMany` |
| Shell async strategy | Dedicated tokio runtime + `block_on` per call — no `.await` in shell |
| Shell transactions | `begin()` / `commit()` / `rollback()` / `savepoint()` — explicit, always rollback on exit |
| Transaction timeout | Optional, developer-chosen at `begin()` — config `transaction_timeout_default` pre-fills prompt (default: 30m) |
| Power loss / crash | Postgres auto-rollbacks dropped connections — no manual recovery needed |
| Shell error handling | Print one-liner + save full traceback to `.djogi_shell_errors/`; session never unwound |
| Shell error log retention | 1 year default; configurable via `error_log_retention` in `Djogi.toml` |
| Shell verbose mode | `djogi shell --verbose` — prints full tracebacks inline in addition to saving to disk |
| Shell history | `.djogi_history` gitignored; `scripts/` committed and shareable |
| Shell import/export | `.export`, `.import`, `.bookmark` — named Rhai scripts in `scripts/` |
| Shell headless execution | `djogi shell --run scripts/name.rhai` — runs script without entering REPL |
| Query engine | Djogi-owned `SqlAccumulator` + `ConditionBuilder` — no third-party query builder |
| Raw escape hatch surface | Raw SQL remains available, but it is no longer an ordinary inherent-method convenience. The raw methods live behind the deliberate bypass harness; see the raw SQL harness decision below and [`raw-sql-escape-hatches.md`](./raw-sql-escape-hatches.md). |
| Raw SQL bypass harness | 2026-05-06: Raw SQL is treated as djogi's `unsafe`. In this repository's tests, use of `raw_*` requires the `#[djogi::deliberately_bypass_convention_with_raw_sql]` attribute and a syntactically attached `// JUSTIFICATION (djogi#<n>):` or `(PIN)` comment. Source-level `djogi::__bypass` references under `tests/` are banned. Tracked at GH #133. |
| CRUD log architecture | Separate `myapp_crud_logs` database; per-model mirror tables; delivery policy derived from a logging profile (`light`, `balanced`, `strict_audit`) with advanced overrides only as escape hatches |
| Event log architecture | Separate `myapp_event_logs` database; `tracing`-powered; severity-routed |
| Log database lifecycle | `db reset` wipes the application DB only — the CRUD-log and event-log databases survive every reset. Future `--wipe-crud-logs` / `--wipe-all-logs` opt-ins are a Phase 8+ concern; today the runner is single-context. |
| Logging UX | Profile-first configuration, not a matrix of operational knobs; maintainers should choose a profile and stop |
| Cross-database logging semantics | Djogi does not promise distributed atomic commit across app, CRUD-log, and event-log databases; stricter audit behavior is enforced by policy, not by pretending the databases commit atomically together |
| CRUD log failure policy | `light` = best-effort, `balanced` = durable bounded retry with health warnings, `strict_audit` = fail-closed for required audit writes |
| Event log failure policy | Best-effort by default under all built-in profiles; outages surface as warnings and metrics, not retroactive app-write failure |
| Admin console | Maahi — Dioxus full-stack on Axum, planned as Phase 10, currently not present in this release branch (`djogi-maahi` crate exists as a planned component only), mounted at `/_admin/` when shipped (see [`docs/spec/maahi/`](./maahi/index.md); HTMX + Askama was an earlier draft direction superseded by the Phase 10 design) |
| Admin form generation | Auto-generated from `ModelDescriptor` — zero per-model UI code required |
| Admin validation | Auto-generated from field annotations + optional `impl AdminClean` hook |
| Admin M2M inlines | Auto-rendered from `impl ManyToMany<T>`; paginated, searchable, configurable |
| Admin opt-out | Per-model `#[model(admin = false)]`; per-field `#[field(expose(none))]` for the absolute field-hiding floor (never UI-rendered, even for superuser, even with `view_full_struct` / `write_full_struct`) per [Maahi — Field Visibility](./maahi/field-visibility.md). `#[field(admin_readonly)]` is a separate widget-render axis (visible but not editable). |
| `Jsonb<T>` in admin | Nested fieldset from schema; unknown fields shown read-only with raw JSON toggle |
| Seed script transactions | Phase 7 ships SQL seeds (`seeds/<database>/*.sql`) routed through `raw_ddl` — operators wrap statements in explicit `BEGIN` / `COMMIT` blocks when they want transactional rollback. The `djogi_seed_runs` ledger records seeds only after the body succeeds; a partial-apply leaves the ledger un-mutated so a re-run picks up where it left off. Rhai-based seeds are deferred to the Rhai shell phase. |
| Query builder — application code | Closure-based: `.filter(\|f\| f.field.gte(val))` — typed, composable, idiomatic |
| Query builder — shell / dynamic | Programmatic: `ModelFilter::new().field(Gte(val))` — serializable, no closures needed |
| Dirty tracking | Off by default; opt-in globally via `Djogi.toml` or per-model via `#[model(dirty_tracking)]` |
| FK cascade default | `RESTRICT` — safest Postgres default; overridden per-field with `#[field(on_delete = "...")]` |
| Field rename detection | `#[field(renamed_from = "old_name")]` — differ treats as rename not drop+add |
| Build drift diagnostic | Plain cargo warning — `build.rs` is diagnostic-only and never writes migration files |
| Migration generation | Explicit via `djogi migrations compose`; `build.rs` detects drift but does not mutate `migrations/` |
| Migration CLI surface | `djogi migrations compose / status / attune` are shipped today (see [Migrations guide § Library APIs](../guide/migrations.md#library-apis)). The `apply / rollback / fake / baseline / verify / repair` CLI dispatchers are deferred to a Phase 7 follow-up; until they ship, library callers reach the runner via the public `djogi::migrate` entry points (`apply_plan`, `rollback_plan`, `fake_apply_plan`, `baseline_plan`, `verify`, and the `repair_*` family). |
| Migration history attunement | `djogi migrations attune [target]` adjusts local migration-history Git state; it may fetch if needed, `--apply` commits ledger/disk reconciliation changes, and it does not execute migration SQL or apply schema DDL. It only updates the parent repo's submodule pointer with explicit `--record` or options that imply recording |
| Migration history squashing | `djogi migrations attune --squash` is dev-gated with the same safety contract as `djogi db reset` and must refuse on shared staging/production history |
| Schema snapshot | Persisted only after a successful run of the library apply path (`djogi::migrate::apply_plan`; the `djogi migrations apply` CLI dispatcher is deferred per the row above) — reflects actual DB state, never build state |
| Migrations folder | Git submodule — pipeline-managed, invisible to developer day-to-day |
| Migration down files | Always generated as a pair; data loss on destructive rollback documented in file |
| Migration runner | Djogi-owned on `tokio-postgres` / `deadpool-postgres` — no `sqlx::migrate` compatibility layer |
| Migration ledger table name | `djogi_schema_migrations` |
| Migration advisory lock key | Hash-derived per migration bucket — `SHA-256("djogi:advisory_lock:" || database || "\0" || app)`, with the first 8 digest bytes interpreted as a big-endian signed 64-bit integer (Postgres `bigint`). Bucket = `(database, app)`; independent buckets hash to distinct keys, so there is no shared literal key and no contention across buckets. The `djogi:advisory_lock:` byte prefix scopes the keyspace so adopter-side advisory locks that hash arbitrary identifiers cannot collide with Djogi's keys. (Phase 7 v3 OQ-06 ruling 2026-04-23, narrowed to per-bucket scope during Phase 7 T4 implementation.) |
| Migration advisory lock connection | **Design intent / required pre-publish hardening:** the lock holder owns a session pinned for the full `apply` / `rollback` / `repair` window (e.g. a dedicated non-pooled `tokio_postgres::Client` checked out and held for the migration's lifetime). Pool reuse cannot silently swap the holder for a session-scoped advisory lock. **Current implementation:** the runner accepts a supplied `&mut DjogiContext` and calls `SELECT pg_try_advisory_lock($1)` / `pg_advisory_unlock($1)` through it; the `DjogiContext` is pool-backed and per-operation checkouts are not yet pinned to a single session for the whole migration window. Acquiring the dedicated-holder invariant the docs aim for is a release-gate hardening item (track ahead of v0.1.0 publish), not a current runtime guarantee. (Phase 7 v3 §3 ruling 2026-04-23, Codex P0-05 fix.) |
| Migration file-level workspace lock | `fcntl` `LOCK_EX` on `<workspace-root>/.djogi-migrations-lock` held exclusively for the duration of `compose` / `attune` / `apply` / `repair` / `baseline`. 30s timeout; D025 on contention. Protects local file state (pending snapshots, `git mv`) that the DB advisory lock does not cover (Phase 7 v3 §6 addition 2026-04-23, Codex P1-06 fix). |
| Migration checksum format | SHA-256 over normalized SQL bytes, stored as `V1:` + lowercase hex digest. Normalization = BOM strip + `\n` line endings + single trailing newline **only** — comments and trailing whitespace are preserved in the hash (Phase 7 v3 Codex P1-04 fix 2026-04-23) so edited review comments surface as drift on `verify`. |
| Migration rollback order | Reverse ledger insertion order (`id` descending), not version-string order |
| Out-of-order migration policy | local/dev allow + warn + record; CI/prod reject by default; override requires explicit flag |
| Three-file snapshot lifecycle | `target/djogi_models.json` (every `cargo build`) → `target/djogi_pending/<target>/<app>.json` (`djogi migrations compose`) → `migrations/<target>/<app>/schema_snapshot.json` (post-successful `apply`). `build.rs` three-way match per (target, app) pair per Phase 7 v3 OQ-10 ruling 2026-04-23. |
| Snapshot `format_version` field | `"1"` at initial ship. Exact string match on load; unknown versions fail fast with a clear upgrade message. Bump only on breaking shape changes; additive fields do not bump (Phase 7 v3 §6 ruling 2026-04-23). |
| Apps subsystem placement | Ships in Phase 7-Zero T7–T10 (per OQ-07 ruling 2026-04-23, reversing the earlier "apps = Phase 7 proper" default). The `djogi::apps!` macro, `AppRegistry`, `AppDescriptor`, lifecycle markers (`renamed_from`, `tombstone`, `moved_from_app`), and the cross-app FK graph are Phase 7-Zero deliverables; Phase 7 consumes them as frozen contracts. |
| Apps declaration syntax | Type-path via unit structs, not string labels. `djogi::apps! { #[app(database = "main")] pub struct Vehicles; }` + `#[model(app = Vehicles)]`. Rust name resolution + sealed `App` trait bound enforce declaration at compile time; undeclared / non-app types fail with standard rustc errors. No cross-crate inventory lookup at macro expansion time (Phase 7-Zero v3 §4B, Codex P0-03 fix 2026-04-23). |
| App identity | `(database, label)` pair, not label alone. Two apps with the same label in different databases are legitimate — `main/audit/` and `crud_log/audit/` are distinct migration directories. Compile-time (same-invocation) and runtime (cross-invocation) uniqueness checks both enforce the pair. `CrossAppEdge` and `AppIdentity` carry both fields. (Phase 7-Zero v3 T7 fixup #3 ruling 2026-04-23.) |
| Apps seal enforcement | Convention at compile time, verified at migration time. True hard-sealing of a proc-macro-emitted trait is not achievable in stable Rust when the macro lives in a separate crate — every pub path the macro reaches is downstream-reachable too. `djogi::apps::__DJOGI_APPS_SEAL_TOKEN` is `#[doc(hidden)] pub`; a determined downstream crate can hand-write `impl djogi::App for Fake` and compile, but the forged impl is inert. Phase 7's migration differ cross-checks every `#[model(app = X)]` against `AppRegistry::all()` before library apply executes and rejects unregistered apps before any SQL executes. (Phase 7-Zero v3 T7 fixup #4 ruling 2026-04-23.) |
| PK-type flip classification | Native Phase 7 work (`SchemaDelta::Classification::PkTypeFlip`), shipped as Phase 7 T9. Distinct from `RequiresLivePlan`, which is reserved exclusively for operator-driven column-level compatibility-window rollouts consumed by Phase 7.5. Rationale: HeeRanjId identity is Djogi's raison d'être; FK-cascading PK migrations are core migration-engine work, not a staged-rollout layer above it (Phase 7 v3 OQ-09 ruling 2026-04-23 + Codex P0-01 fix). |
| Online-safety classification enum (Phase 7.5) | Four variants — `OnlineSafe` (no live plan), `FastLockDestructiveGuarded` (short-lock but destroys data / invalidates dependents; gated by `--allow-destructive`), `ExpandContract` (Phase 7.5 live plan via `RequiresLivePlan` handoff), `OfflineOnly` (refused). `DROP COLUMN` is `FastLockDestructiveGuarded`, not `OnlineSafe`. Volatile defaults downgrade column-add from `OnlineSafe` to `ExpandContract`. Postgres-18-fast-path rows are hard dependencies, not optional optimizations (Phase 7.5 v3 §7 ruling 2026-04-23, Codex P1-01 fix). |
| Concurrent index creation | Deterministic A (Phase 7-Zero v3 Q1 ruling 2026-04-22). `concurrently = true` at declaration sets `IndexSpec::requires_out_of_transaction = true`; runner emits `CREATE INDEX CONCURRENTLY` in every profile (prod, CI, dev, test); containing migration is non-transactional in every profile. No profile-gated behavior. Framework does not auto-detect — operator responsibility. Apply-time advisory warning (Phase 7-Zero v3 §6.5) surfaces the foot-gun without changing DDL. |
| Unique constraint vs unique index default | Unique constraint is the default for ordinary uniqueness. Unique index only when Postgres requires it: partial (`predicate`), expression-target, `include`, or `nulls_not_distinct = true`. The macro picks; user does not have to know the distinction for common cases (Phase 7-Zero v3 §2 item 2 + §6.1 lowering rules). |
| Index column ordering | Column order is semantic — preserved byte-for-byte in descriptor, SQL, canonical name, and diff. Never sorted. `index(fields = [last, first])` and `index(fields = [first, last])` are different indexes with different names (Phase 7-Zero v3 §2 item 3). |
| Index column spec | Per-column opclass + sort direction + nulls ordering via `IndexColumnSpec { name, opclass, order, nulls }` on `IndexTarget::Columns(&[IndexColumnSpec])`. `IndexColumnSpec::simple(name)` const-fn constructor keeps the common case ergonomic. Top-level `IndexSpec::opclass` is removed — per-column opclass supersedes it (Phase 7-Zero v3 §4, Codex P1-02 fix 2026-04-23). |
| Index predicate validation | Raw SQL fragment (e.g. `"deleted_at IS NULL"`); Djogi does not parse or shape-validate predicates. Postgres parses at migration time; invalid SQL surfaces as a migration error with the full server-side diagnostic. No regex, no DSL (Phase 7-Zero v3 §3, Codex P0-04 fix 2026-04-23). |
| `djogi migrations attune --squash` publication flag | `--publish` (not `--push`). Abstraction consistency with `attune` (both djogi verbs, not mixed with git vocabulary); communicates the consequence (team sees the rewrite) rather than the mechanism; matches `cargo publish` ecosystem vocabulary (Phase 7 v3 OQ-04 ruling 2026-04-23). |
| `djogi docs` output layout | One Markdown file per registered model under `<output>/<app>/<Model>.md`, plus a top-level `<output>/README.md` index grouped by app (sorted alphabetically; the synthetic global bucket appears under the on-disk token `_global_`). Output is byte-deterministic against the same descriptor inventory — no embedded timestamps or environment-derived data — so two runs against the same compiled binary produce identical files. Default output root: `target/djogi-docs/`. (Phase 7 T8 ruling 2026-04-25.) |
| Composite migration boundary | Composite unique constraints and composite indexes are in `0.1.0`; composite primary keys are not |
| Multi-database migration contract | Migrations are target-scoped at the ledger layer and bucket-scoped at the advisory-lock layer: each database target has its own `djogi_schema_migrations` ledger (one per target, shared across all apps in that target), and each `(database, app)` bucket gets its own advisory-lock key (per "Migration advisory lock key" row above) so independent buckets in the same target do not serialize against each other. Snapshots are finer-grained — per `(target, app)` per the "Three-file snapshot lifecycle" row — and each bucket's plan acquires its own lock. Djogi does not promise distributed atomic migration across targets. |
| Cross-database foreign keys | Explicitly rejected — ORM `ForeignKey<T>` is same-database-target only; cross-target references must be scalar/application-level references |
| Database target | Postgres only — permanent decision, not a limitation; enables JSONB, HeeRanjId, advisory locks, transactional DDL, `RETURNING` |
| Postgres version floor | Postgres 18 — no support for older versions. Rationale: Djogi is pre-publish and unapologetic about adoption shape; teams migrating an existing app to Djogi have substantial app-side work regardless, so bundling a Postgres upgrade is a small marginal cost. The framework will freely use any Postgres 18+ feature (extended protocol niceties, latest JSONB work, `MERGE`, logical replication, generated-column expressiveness) without version-gating fallbacks. |
| Dev database reset | `djogi db reset` — triple-gated: localhost-resolving `DATABASE_URL`, `Djogi.toml::profile != "production"`, and explicit confirmation (`--yes` flag or interactive `yes` answer). Drops + recreates the application DB through the maintenance database (`postgres` by default; overridable via `--maintenance-database`), then replays committed migrations through the runner's normal `apply_plan` path. Logging databases are NOT touched. (Phase 7 T8 ruling 2026-04-25.) |
| CLI interface | `djogi` CLI — installed via `cargo install djogi-cli` |
| Djogi's scope | Model derivation chain only — does not duplicate `tokio-postgres`, HeeRanjId, Tokio, or any Rust web framework's responsibilities. `axum` is the best-covered framework example today (opt-in via the `axum` feature flag); other frameworks integrate through their own per-framework flags or manual wiring. |
| Public requirement translation | Private app requirements may inform Djogi, but specs/docs describe them only as product-agnostic framework capabilities |
| Core vs companion crate boundary | Djogi keeps reusable data-layer primitives; domain policy, workflow logic, and specialized integrations belong in app crates or companion crates |
| `Jsonb<T>` field type | `JSONB` column with typed schema, serde deserialization, validator validation, nested schema support |
| Unknown field preservation | Fields not in schema loaded into `extra: IndexMap<String, UnknownField>` — never dropped on save |
| `UnknownField` variants | `String`, `Bool`, `Float`, `Int`, `Null`, `RawJson` — no implicit coercion between types |
| `UnknownFieldError` | `FieldNotFound`, `TypeMismatch`, `NoImplicitCoercion` — all conversions return `Result` |
| Nested `Jsonb<T>` | Fully supported — each nesting level has its own typed schema and unknown field boundary |
| JSONB subfield queries | Typed filter accessors generated per known field using Postgres JSONB path operators |
| CRUD logging | Optional per-model or global; stored as per-model `{snake_case(model)}_logs` mirror tables in the separate `myapp_crud_logs` database (see [Logging](./logging.md) §9.1); off by default (a single shared `_djogi_crud_log` table was an earlier draft direction superseded by the per-model mirror layout) |
| CRUD log JSON diffing | Dot-notation paths through full `Jsonb<T>` nesting depth including unknown field changes |
| CRUD log actor | Optional `save_with_actor()` or request-context hook; null if not provided |
| Project scaffolding | `djogi new` — scaffolds project and initializes migrations submodule |
| Regex anywhere in djogi (Rust side) | **No Rust regex engine, ever — no exceptions.** No regex-engine dependency (`regex`, `regex-lite`, `fancy-regex`, `regex-automata`, or equivalent) may enter any workspace crate (`djogi`, `djogi-macros`, `djogi-cli`, `djogi-shell`, or any future crate). Framework-internal pattern checks (identifier validation, DDL emission shape checks, anything Djogi enforces at compile or migration time) are expressed in plain English and implemented with stdlib byte primitives: `u8::is_ascii_alphabetic`, `u8::is_ascii_alphanumeric`, explicit byte equality, sorted const slices with `binary_search`, stack-allocated `[u8; N]` buffers, and similar — never via regex notation in doc comments, commit messages, or other in-repo text. Regex is heavy, hides intent behind a DSL most readers re-parse every time, and invites per-query allocation; that critique applies to *Rust-side* regex use, which the rule still bans absolutely. Regex notation in prose is no easier to skim than the underlying rule written out — spell the rule out in words (e.g. "ASCII letter or underscore followed by ASCII alphanumerics or underscores, up to 63 bytes"). |
| Postgres POSIX regex operators (carve-out) | **Permitted as a Postgres feature.** Postgres exposes POSIX regex matching as the `~` and `~*` SQL operators; Djogi surfaces them through the typed query API as `FieldRef::regex(s)` (`col ~ $1`) and `FieldRef::iregex(s)` (`col ~* $1`), and through the programmatic builder as `Lookup::Regex(String)`. The carve-out is narrow and deliberate: the predicate runs entirely server-side, no Rust regex code is linked, and the adopter is composing a Postgres query — not invoking framework-internal regex. The carve-out covers only the `~` / `~*` operators reached through the typed surface or `ctx.raw_*`; framework-internal text matching never reaches for regex even where Postgres supports it. |
| Spatial SRID | Locked to `GEOGRAPHY(Point, 4326)` in Phase 6. Non-4326 work goes through raw SQL (`ctx.raw_execute(...)`) combined with `FieldSqlType::Custom(&'static str)` on the field type. A future `GeoPoint<const SRID: u32>` generalization is a candidate if real adoption pressure emerges — it would be an additive, non-breaking change per the pre-publish policy. |
| Spatial codec | Manual EWKB encode/decode in-repo (`djogi/src/geo/ewkb.rs`) — 25-byte little-endian layout (endian marker, type word with SRID flag, SRID, X longitude, Y latitude). No new dependency. A future dep swap to `geozero` or equivalent is a single-file change because all raw-byte logic is isolated in `ewkb.rs`. |
| Spatial feature-flagging | `spatial` feature flag on the `djogi` crate (not a separate crate, per the one-djogi-crate rule). Default builds do not compile any PostGIS surface. The `djogi::geo` module and the `djogi::GeoPoint` re-export are both gated on `#[cfg(feature = "spatial")]`. |
| Spatial query IR | `SpatialExpr` nodes live under Phase 4's `ExprNode` enum as a single `ExprNode::Spatial(SpatialExpr)` variant. No parallel `Condition::Spatial` arm. This keeps filter, order, annotate, and aggregate pipelines on one emitter walk. |
| Spatial ordering determinism | `order_by_distance` appends the primary-key column (`id`) as an unconditional ascending tiebreak. Equidistant rows sort by ascending primary key — reproducible across repeated executions and safe for keyset pagination. |
| Spatial constructor policy (v0.1.0 anchor) | `ST_*` and related constructors are intentionally deferred to post-v0.1.0 unless adopted by explicit shape requirements, with `ST_TileEnvelope` escalatable through Cluster 4C if MVT/Geobuf row-shape work in [#92](https://github.com/TarunvirBains/djogi/issues/92) requires it. Specialized constructors remain behind raw SQL bypass in v0.1.0, including `ST_TileEnvelope`, `ST_HexagonGrid`, `ST_SquareGrid`, `ST_Letters`, `ST_MakePointM`, `ST_MakeValid`, `ST_IsValidDetail`, `ST_IsValidReason`, plus long-tail clustering/coverage/trajectory/I/O constructors. Tracked in [#179](https://github.com/TarunvirBains/djogi/issues/179) and [`docs/research/postgres-coverage/2026-05-09/02-postgis-functions.md`](../research/postgres-coverage/2026-05-09/02-postgis-functions.md). |
| Non-point spatial geometries (Phase 6.5) | `GeographyValue` trait is sealed; ships concrete impls for `GeoPoint`, `LineString`, `Polygon`, `MultiPoint`, `MultiLineString`, `MultiPolygon`. Each has a manual EWKB codec isolated in `djogi/src/geo/ewkb.rs` — no new dependency. OGC simple-feature invariants (closed rings, minimum-point counts) validated at constructor time; `postgres_types::{ToSql, FromSql}` impls route through the same EWKB layer. |
| Spatial shape-predicate cast discipline (Phase 6.5) | `ST_Intersects` has a native `geography` overload, so Djogi emits `ST_Intersects(<col>, $n::bytea::geography)` with no column cast. `ST_Contains` / `ST_Touches` / `ST_Within` have no geography overload in PostGIS 3.x, so both sides are cast to `geometry`: `ST_<Func>(<col>::geometry, $n::bytea::geometry)`. The `$n::bytea::<type>` double-cast is required because `Vec<u8>: ToSql` binds as `bytea`; a direct `$n::geography` cast makes `tokio_postgres` prepare `$n` as `geography` and reject the `Vec<u8>` value at prepare time. |
| Spatial region-JOIN function choice (Phase 6.5) | `group_by_region` / `count_by_region` emit `LEFT JOIN … ON ST_Covers(r.<r-geo-col>, t.<t-geo-col>)` — not `ST_Contains`. Two reasons: (1) `ST_Covers(geography, geography)` exists in PostGIS 3.x, `ST_Contains(geography, geography)` does not, so `ST_Covers` avoids `::geometry` casts under the JOIN and keeps GiST-indexed bbox prefiltering active on the geography column; (2) `ST_Covers` is boundary-inclusive (a point on a polygon's edge is "covered") whereas `ST_Contains` is interior-only (the same edge point is "not contained"). The two functions agree for points strictly inside or outside the polygon and differ only on the boundary — and for spatial grouping, the boundary-inclusive interpretation is the useful one: a point on a neighborhood edge should still count under *some* neighborhood rather than falling into the `None` bucket. |
| Spatial cluster emitter subquery wrap (Phase 6.5) | `cluster_by_proximity` wraps the `ST_ClusterDBSCAN(...) OVER ()` call in an inner `FROM (SELECT t.*, ...) AS t` subquery so the outer `GROUP BY cluster_id` references a materialised column. The flat form `SELECT ST_ClusterDBSCAN(...) OVER () AS cluster_id ... GROUP BY cluster_id` is rejected by Postgres with `ERROR: window functions are not allowed in GROUP BY`. |
| Spatial grouping source enum (Phase 6.5) | The three spatial grouping shapes (Join / Cluster / Geohash) unify under a single `SpatialGroupSource` variant on `GroupedAnnotatedQuerySet`. Dispatch to the appropriate SQL builder happens in `build_grouped_annotated_select`. New shapes are added as new variants — the grouped-substrate contract (key-tuple emission, aggregate-tuple emission, HAVING / ORDER / LIMIT tail) is identical across all three. |
| Grouped queryset type-state (Phase 6.5) | Grouping transitions through three type-states: `QuerySet<T>` → `GroupedQuerySet<T, K>` (via `group_by` / `rollup` / `cube` / `group_by_sets`) → `GroupedAnnotatedQuerySet<T, K, A>` (via `.annotate(...)`). Each stage locks in what downstream operations are allowed — you cannot fetch a `GroupedQuerySet` without first annotating, you cannot call `group_by` on an already-grouped queryset. The sealed `IntoGroupKeyTuple` and `IntoAggregateTuple` traits cover arity 1–4 in this phase; exceeding the cap raises a clear compile error. |
| Window aggregate without `group_by` (Phase 6.5) | `.annotate(...)` on a plain `QuerySet<T>` emits the aggregate with `OVER ()`, producing one row per base table row with the table-wide aggregate attached. `.annotate(...)` after `group_by` omits the `OVER` clause and emits a plain `GROUP BY` aggregate. One method, two emission modes keyed on type-state — no separate `.window(...)` entry point. |
| Aggregate alias discipline (Phase 6.5) | Aggregates are aliased `__djogi_agg_N` (N starting at 0). User-supplied SELECT aliases that collide with the synthetic alias are caught at SQL-build time by `assert_no_alias_collision()` (the generic SELECT-list duplicate-alias detector at `djogi/src/query/sql.rs:1559`), which returns `DjogiError::AliasCollision { alias }`. The check is generic across any duplicate alias; it indirectly enforces the `__djogi_agg_*` reservation because the framework always emits the synthetic alias and a user duplicate would surface as a SELECT-list collision. The synthetic alias prevents user renames from breaking positional decode. |
| `#[djogi_test(extensions = [...])]` (Phase 6.5) | The test harness macro accepts an `extensions = ["..."]` array. Each per-test database runs `CREATE EXTENSION IF NOT EXISTS "<name>"` after HeeRanjID install and before user setup. Extension names validated with a byte-level ASCII-identifier check — no regex (per the no-regex rule), no database round-trip for validation. Invalid names fail at macro expansion with a span-precise `syn::Error`; valid-but-nonexistent extensions surface as `DjogiError::Db` at runtime. |
| Two-phase constraint validation default (Phase 7.5) | **Planned/target:** `CHECK`, `NOT NULL`, and `FOREIGN KEY` constraints added to tables exceeding `live.validation_threshold_rows` (default 100k) are emitted as `ADD CONSTRAINT … NOT VALID` followed by a separate `VALIDATE CONSTRAINT` step. The `NOT VALID` step takes a fast catalog lock; `VALIDATE` runs under `ShareUpdateExclusiveLock` (concurrent reads + writes allowed). Planned operator path: `djogi live finalize`; in v0.1.0-alpha this command is documented but stubbed/deferred. Current v0.1.0-alpha apply path (outside `live finalize`) still emits the single-statement `ADD CONSTRAINT` form for CHECK / NOT NULL and inherits `AccessExclusiveLock` for populated-table validation. Below the threshold, the behavior is the same. **Exclusion constraints are not eligible** — Postgres 18 does not accept `NOT VALID` for `EXCLUDE …` constraints, so adding an exclusion constraint to a populated table classifies as `OfflineOnly` (operator must accept the `AccessExclusiveLock` window or rebuild via the unique-via-index pattern when the constraint reduces to uniqueness). **Lens:** scalability — large-table validation for the supported constraint kinds no longer blocks production once the live-runner path is implemented; production stability — `AccessExclusiveLock` windows are bounded in the planned live path and never emitted for Postgres-rejected SQL. |
| Unique constraint via concurrent index promotion (Phase 7.5) | When adding a unique constraint to a populated table classified `ExpandContract`, the live plan emits `CREATE UNIQUE INDEX CONCURRENTLY` then `ALTER TABLE … ADD CONSTRAINT … USING INDEX`. The promotion `ALTER` takes a brief `AccessExclusiveLock` (sub-millisecond on Pg18); the index build runs lock-light. Same shape applies when the differ produces a `DROP INDEX` + `CREATE INDEX` pair on overlapping columns: both sides must be `concurrently = true` or the operation refuses. **Lens:** scalability — uniqueness enforcement on large tables no longer requires downtime; production stability — index-replacement no longer leaves the table without an index window. |
| Three-step column-with-default add (Phase 7.5) | Postgres-VOLATILE `DEFAULT` expressions (`clock_timestamp()`, `random()`, `gen_random_uuid()` — the formal `VOLATILE` category that Pg18's fast-path explicitly does NOT cover) route through a 3-step live plan: (1) `ADD COLUMN … NULL` + install a write-side trigger that populates the column for INSERTs / UPDATEs landing during the window, (2) `ALTER TABLE … ALTER COLUMN … SET DEFAULT <expr>` so new rows get the default natively, (3) chunked backfill of historical rows via the standard backfill substrate. Stable / immutable defaults — including `now()`, `CURRENT_TIMESTAMP`, `transaction_timestamp()` (all formally STABLE), string / integer literals, and immutable function calls — remain `OnlineSafe` (catalog-only on Pg18). Postgres `DEFAULT` clauses cannot reference other columns; that requirement falls out of scope for this row and routes through generated columns or a custom transform instead. **Volatility resolution is compose-time and host-independent:** Djogi ships a static table of every Pg18 built-in function's `provolatile` category baked into the classifier (`djogi/src/migrate/pg_volatility.rs`); unknown identifiers — user-defined functions, extension functions, qualified `schema.func` calls Djogi can't classify — default conservatively to `VOLATILE` and route through 3-step staging. Adopters with a known-stable user-defined function can override via `#[field(default_volatility = "stable")]`. The classifier never reads live `pg_catalog`; compose remains pure and host-stable, so a UDF installed on staging but not production produces the same plan on every host. **Lens:** scalability — never rewrites the table in place when staging is needed; production stability — host-independent classification eliminates a class of "works in staging, blocks in production" surprises; correctness — only emits patterns Postgres accepts; simplicity — one static table, one explicit override knob. |
| Per-migration timeouts (Phase 7.5) | Every migration session sets `statement_timeout` and `lock_timeout` at the start of work and restores at the end. Defaults locked: `statement_timeout = 1h`, `lock_timeout = 2s`. `lock_timeout = 2s` deliberately favors refusal over indefinite queueing — a stuck migration that holds `AccessExclusiveLock` past 2s gets rejected, freeing the table for production traffic. Per-migration overrides via header pragmas (`-- djogi: statement_timeout=10m`); per-project defaults via `Djogi.toml` `[live] statement_timeout` / `lock_timeout`. **Lens:** production stability — bounded blast radius on every migration; simplicity — one defensible default everywhere. |
| Exponential lock-retry on contention (Phase 7.5) | When a migration session is refused due to `lock_timeout`, the runner retries with exponential backoff. Default policy: 4 attempts at 1s/2s/4s/8s initial delay (configurable via `[live] lock_retry_attempts` / `lock_retry_initial_ms`). Eliminates spurious failures from brief lock contention without masking persistent contention. Retry only on `lock_timeout`-class errors; other failures abort immediately. **Lens:** production stability — operator no longer manually re-runs on transient contention; simplicity — one tuning knob for the rare case. |
| Justification ledger field for destructive operations (Phase 7.5) | When destructive SQL is applied through the migration library or any command emitting `Lossy` / `Destructive` SQL, `--justify "<reason>"` is required and persisted in `djogi_schema_migrations.justification` (TEXT, NOT NULL when the row's classification is destructive). The same gate applies at the live-plan level: any plan whose step graph contains a destructive step requires `--allow-destructive --justify` at future `djogi live run` time, and the justification is copied into every ledger row that step ultimately produces. For `attune --record` of hand-edited migrations containing detected dangerous statements, the justification is stored as a `-- djogi-justify: <reason>` header in the migration file itself at record time (since `attune --record` does not mutate the live DB) and copied into the ledger when the migration is later applied. No silent destructive operations; every destructive ledger row carries an audit-trail reason regardless of which path produced it. **Lens:** production stability — every destructive change is reviewable post-incident, no matter which command surfaced it; simplicity — one flag, one column, one persistence path; correctness — `attune --record` doesn't pretend to write to a ledger it has no business mutating. |
| Backfill side-effect suppression default (Phase 7.5) | While a live-plan `BackfillChunked` step writes rows, `#[model(events)]` outbox emission and other framework write-hooks are suppressed by default. Backfilling 1M rows must not fan out 1M domain events to downstream consumers. Suppression is implemented by `SET LOCAL "djogi.live_migrate.suppress_events" = '1'` inside each per-chunk transaction, scoped to the live-plan step's transaction; ordinary application writes outside the migration session are unaffected. The public Rust entry points `djogi::live_migrate::execute_backfill` and `djogi::live_migrate::resume_backfill` expose an `emit_events: bool` parameter — an advanced, low-level surface that adopter integration code (or tests) can pass `true` to deliberately keep hooks firing for a backfill window. Off by default at every call site that does not explicitly opt in. **No high-level adopter-facing opt-in is exposed today**: no `--emit-events` CLI flag on `djogi live run` / `resume`, no `#[migration(emit_events_during_backfill)]` proc-macro attribute (no `#[migration]` macro exists yet at all), and no derive-level surface. A high-level opt-in is deferred; for adopter-driven backfills routed through the CLI / derive contract, suppression is always-on until that surface lands. **Lens:** scalability — prevents event-storm on large backfills; production stability — downstream consumers don't receive synthetic schema-change traffic; honesty — the contract documents what ships today (a low-level Rust parameter, no high-level CLI / derive contract) rather than advertising flags or attributes that don't exist. |
| Hash-index hazard refusal (Phase 7.5) | The classifier refuses `IndexType::Hash` declarations that don't have `requires_out_of_transaction = true` set, with a `IndexBuildHazard::HashRequiresConcurrent` diagnostic at compose time. Hash indexes have transactional limitations on Pg18 (cannot be built inside a transaction in some configurations) that bite operators silently otherwise. Already-present hash indexes in the live DB are not affected. **Lens:** production stability — eliminates a class of foot-gun before SQL ever runs. |
| Multi-FK staging threshold (Phase 7.5) | When a single `(database, app)` bucket diff produces 4+ `ADD CONSTRAINT … FOREIGN KEY` operations or 4+ `DROP CONSTRAINT` operations targeting FKs on a single table, classification escalates to `ExpandContract` and the live plan stages them across multiple steps with interleaved `VALIDATE CONSTRAINT` calls. Threshold (default 4) configurable via `[live] multi_fk_threshold`. Same threshold applies to dropping a table with 4+ inbound FKs — but the trailing `DROP TABLE` step retains its destructive classification and requires `--allow-destructive --justify "<reason>"` independently of the staging plan. The orthogonality is structural: `ExpandContract` describes *how* steps are sequenced; destructive gating describes *what* approvals each step needs. A live plan whose graph contains any step emitting `Destructive` or `Lossy` SQL must be invoked with both gates passed at the plan level. **Lens:** scalability — prevents lock-storms on heavily-referenced tables; production stability — bounded lock concurrency *and* destructive operations cannot smuggle past gating by hiding inside a live plan. |
| Stored generated column rewrite classification (Phase 7.5) | Adding or changing a `GENERATED ALWAYS AS (<expr>) STORED` column on a populated table rewrites every row under `AccessExclusiveLock` — and creating the *replacement* generated column also rewrites the table, so a shadow-column pattern does NOT save the operator from the lock window. Classified `OfflineOnly` for populated tables; operator must accept the maintenance window or migrate the column to a regular column with an application-level fill (which routes through the 3-step column-with-default pattern instead). Virtual generated columns (Pg17+) are catalog-only and remain `OnlineSafe`. Replacement-column expand/contract still applies if the operator chooses to remodel the column away from STORED-generated entirely; that's a regular column transition. **Lens:** correctness over wishful thinking — Djogi does not pretend to make table-rewrite-required DDL online when Postgres semantics make that impossible; production stability — the `OfflineOnly` refusal forces operator acknowledgement instead of a silent multi-minute lock. |
| `check_down` opt-in for down-migrations (Phase 7.5) | Down-migrations run through the same `OnlineSafety` classifier when `[live] check_down = true` is set in `Djogi.toml` (default: `false`). High-stakes deployments opt in; default-off keeps dev rollback ergonomic. When enabled, rollback execution refuses the rollback if the down-migration emits `Destructive` / `Lossy` SQL without `--allow-destructive --justify`. **Lens:** production stability — destructive rollbacks get the same gating as forward changes for teams that want it; simplicity — opt-in keeps default behavior unchanged. |
| Live plan multi-app boundary (Phase 7.5) | A single live plan operates within one `(database, app)` bucket — same boundary as Phase 7's per-`(database, app)` advisory-lock bucket (see "Migration advisory lock key" row). Cross-app live plans are refused at compose time. When a single descriptor diff legitimately spans multiple buckets, Phase 7's compose phase splits it per-bucket and Phase 7.5 materializes one independent plan per bucket; ordering between those plans remains the operator's responsibility. In the future live-runner surface, each plan is run via its own `djogi live run <plan-id>` invocation and the operator decides the sequence; in v0.1.0-alpha, that command body is documented/stubbed and not shipped as an operational runner. Plan-to-plan dependency machinery is out of scope for v1 — operators with strict ordering needs use shell scripts or per-environment runbooks. The `djogi_live_plans` table carries `target_database` + `app_label` columns for bucket scoping. **Lens:** scalability — per-bucket lock semantics carry forward from Phase 7; simplicity — operator-driven ordering keeps Djogi out of the "build a workflow engine" business until adoption pressure surfaces a concrete need. |
| Three-DB live-plan applicability (Phase 7.5) | Event-log database evolves via Phase 7 migrations directly — no `ExpandContract` paths, no operator-driven rollouts; event-log writes are best-effort under all built-in profiles per the existing `Event log failure policy` row, so brief `AccessExclusiveLock` windows there don't block production. **Crud-log database respects the active CRUD logging profile.** Under `light` and `balanced` profiles (best-effort + bounded retry), crud-log migrations route directly to Phase 7 the same way event-log does. Under `strict_audit` (fail-closed), crud-log migrations on populated tables route through Phase 7.5's classifier the same way the application DB does — because under fail-closed semantics, a multi-second `AccessExclusiveLock` on a crud-log mirror table blocks every `INSERT` / `UPDATE` / `DELETE` against the corresponding application table. The classifier inspects both `target_database` and the resolved logging profile. **Lens:** production stability — fail-closed audit guarantees aren't undermined by direct-mode log-DB migrations; simplicity — adopters who chose `strict_audit` get safe migration semantics for free; correctness — the classifier respects the audit contract Djogi already promises. |
| PK-flip vs online-safety classifier boundary (Phase 7.5) | `Classification::PkTypeFlip` is **not** routed through the online-safety classifier. PK-flip migrations are inherently long-running and unavoidably touch every cascading FK; their orchestration lives in Phase 7 T9 with the cascade emitter family. The online-safety dimension applies only to column-level operations on tables not undergoing PK-flip. Phase 7.5's `RequiresLivePlan` handoff and `PkTypeFlip` are mutually exclusive — a single delta cannot carry both classifications. **Lens:** simplicity — one orchestration owns PK-flip end-to-end; production stability — no double-routing of PK-flip work. |
| Custom-PK-type flip support (deferred — superseded 2026-05-12) | **v0.1.0 status:** explicit reject (djogi#165). Phase 7 T9 ships PK-flip emitters for the four built-in HeeRanjId variants only (`HeerId`, `HeerIdRecencyBiased`, `RanjId`, `RanjIdRecencyBiased`). The descriptor differ detects custom-PK transitions and emits `SchemaOperation::Unsupported` with an operator-readable diagnostic that names the table, classifies the bucket (`custom-to-custom`, `built-in-to-custom`, `custom-to-built-in`), and surfaces `type_name` / `sql_type` / `default_sql` for the custom side(s); compose then refuses with `ComposeError::UnsupportedDelta`. The earlier Phase 7.5 plan — a `CustomPkFlip` cascade emitter routed through a `Classification::PkTypeFlip { kind: Custom }` variant with `flip_to: &[CustomPkKind]` macro validation — was scoped out of v0.1.0 because every custom shape change requires adopter-specific decisions (value-preserving cast across heterogeneous SQL types, FK cascade strategy, DEFAULT generator's bulk-allocation contract) that the framework cannot derive on its own. See `docs/spec/migrations.md` §10.10a for the v0.1.0 support matrix and rationale; the post-v0.1.0 extensibility path remains open as adoption pressure surfaces concrete shapes. **Lens:** correctness — v0.1.0 ships a typed reject the operator can act on, not a generic debug dump that obscures what changed; production stability — silently degrading custom shape changes to `ALTER COLUMN TYPE` would risk truncating row IDs when inner SQL types differ; simplicity — one explicit gate per identity field. |
| Visage compat-window contract (Phase 7.5) | During `ExpandContract` compatibility windows, visage `SELECT` projections (Phase 4.5 / Phase 7-Zero-2) are pinned to the *contracted* schema (post-cutover column set). Adopters running queries during the compat window that need the legacy projection must opt in via `#[visage(during_migration = legacy)]` on the visage definition. Default is forward-looking so post-cutover queries don't surprise. The `DjogiVisageOf<M>` boundary (Phase 7-Zero-2) carries the migration-aware projection through the compile-time contract. **Lens:** production stability — query shape doesn't shift on cutover for the default case; simplicity — one knob for the rare opt-in. |
| Backfill resume substrate scope (Phase 7.5) | Resume-on-restart for backfills is two-tier: (tier-1) **planned operator-driven** via future `djogi live resume <plan-id>` — single-process, no daemon, picks up at `backfill_rows_done`, can advance through any plan step including operator checkpoints (`CutoverReads`, `CutoverWrites`, `FinalizeConstraints`); (tier-2) **planned daemon mode** via future `djogi live daemon` — long-running process polling `djogi_live_plans` and resuming **only resume-safe steps** (`BackfillChunked`, `ValidateBackfill`) whose row is `running` with stale `last_progress_at`. Daemon mode never advances through `paused` rows (the operator's checkpoint state) and never crosses `CutoverReads` / `CutoverWrites` / `FinalizeConstraints` — those require explicit operator authorization. Daemon coordination uses Postgres advisory locks keyed on `SHA-256("djogi:live-plan:<plan-id>")` (first 8 bytes as big-endian `i64`, parallel to the migration advisory lock construction in the existing `Migration advisory lock key` row). **Design intent / required pre-publish hardening:** the holder pins a non-pooled `tokio_postgres::Client` (or otherwise dedicated session) for the full resume window so pool reuse cannot silently swap the holder for a session-scoped advisory lock. **Current implementation:** the daemon substrate's `try_acquire_advisory_lock` / `release_advisory_lock` helpers accept a supplied `&mut DjogiContext` (pool-backed); the same per-operation checkout gap called out in the existing `Migration advisory lock connection` row applies here. Pinning the resume window to a single non-pooled session is a release-gate hardening item, not yet enforced. Multi-process / scheduler-driven coordination across machines is deferred to Phase 11 (Distributed Topology). **Lens:** scalability — long-running backfills survive operator crashes; production stability — daemon never advances cutover/finalize gates without operator action, and lock semantics mirror the Phase 7 advisory-lock construction (the dedicated-holder hardening on both layers is tracked as one release-gate item); architectural — distributed coordination stays in its proper phase. |
| Custom-checks extensibility surface (Phase 7.5) | Adopters can register custom dangerous-op refusal rules via `djogi::live::register_check`. Signature: `fn(SchemaOperation) -> CheckResult` where `CheckResult::Refuse { reason: &'static str }` aborts the compose with the supplied reason. Registry is compile-time `inventory::collect!` (consistent with the apps + descriptor pattern); runtime registration is rejected. Per-org rules ship as crates that submit checks via `inventory::submit!`. **Lens:** scalability — large orgs can encode their own policies without forking; idiomatic Rust — same inventory pattern as descriptors / apps; simplicity — closed-form classifier for default users, extensible for those who need it. |
| `djogi db cleanup-test-dbs` CLI (Phase 7.5) | Wraps the existing `djogi::testing::cleanup_orphaned_test_databases` library helper as a CLI subcommand. Sweeps `djogi_test_<uuid>` orphans surviving process-abort scenarios from `#[djogi_test]` runs. Triple-gated identical to `djogi db reset` (localhost + non-production + `--yes`). Includes `--dry-run` to list candidates without dropping. **Lens:** simplicity — dev workflow ergonomics; production stability — same triple-gate as the existing reset path. |
| CI test-job parallelization with fail-fast (Phase 7.5) | The integration-test job splits into two parallel matrix jobs by binary partition (matrix `[a, b]` where each runner takes ~half the integration test binaries). Bounded at 2 to fit GH Actions runner budget per the established CI ceiling. `fail-fast: true` cancels remaining matrix legs on any leg's failure. Test-binary partitioning is deterministic per the alphabetic order of the `[[test]]` entries in `djogi/Cargo.toml`. Closes GitHub issue #43. **Lens:** scalability — test signal time roughly halves; production stability — faster CI → faster release iteration. |
| Spatial cfg flag in djogi-macros (Phase 7.5) | `djogi-macros/Cargo.toml` declares `[features] spatial = ["djogi/spatial"]` so direct macro tests compile against the matching `djogi` public API surface. The spatial compile-pass fixtures are gated with `#[cfg(feature = "spatial")]` and run through the named lihaaf `spatial` suite, which enables `features = ["spatial"]` for those fixtures; the macro crate itself still has no spatial-specific code paths. (Historically, the spatial fixtures lived in a dedicated `tests/trybuild_spatial_tests.rs` driver that enabled a child `rustc --cfg 'feature="spatial"'`; that driver was removed when lihaaf replaced trybuild in Phase 8.5.) **Lens:** production stability — clean CI signal and feature-coherent compile fixtures. |
| Live-plan dashboard deferral (Phase 10 / Maahi) | Operator-facing UI for in-flight live plans (progress bars, current step, last-error display, manual abandon button) is deferred to Phase 10 as part of Maahi admin. `djogi live` command shape is declared (`show`, `status`, `run`, `resume`, `finalize`, `abandon` + daemon-mode resume), but these remain stubbed / deferred in v0.1.0; `status` is not registered in shipped CLI and core `run` / `resume` / `finalize` paths are not implemented in the current release. Maahi will consume the existing `djogi_live_plans` table via the standard `#[model]` admin-render pipeline after that phase. **Lens:** simplicity — Phase 7.5 documents migration control before it ships; architectural — UI surface belongs in the admin phase. |
| Multi-replica cutover deferral (Phase 11 / Distributed Topology) | Replica-lag-aware cutover timing — pause cutover until all read replicas have caught up to a target LSN — is deferred to Phase 11 (Distributed Topology). Phase 7.5 cutover commands assume single-primary semantics; the `CutoverReads` / `CutoverWrites` step graph emits the SQL but does not coordinate with replica state. Multi-replica coordination requires the broader replication-aware connection routing that Phase 11 owns. **Lens:** architectural — replica-lag belongs with the rest of distributed topology; production stability — single-primary cutover ships ready, multi-replica gets first-class treatment when its phase lands. |
| Reserved identifier namespace (Phase 8-Zero) | The `__djogi_` prefix is reserved ASCII-case-insensitively for framework-internal SQL identifiers (recursive CTE names like `__djogi_tree` / `__djogi_closure`, derived-table aliases like `__djogi_q`, synthetic column aliases like `__djogi_agg_N` / `__djogi_edge_label` / `__djogi_search_seq` / `__djogi_parent_id`) and macro-emitted scratch identifiers in user crate scope (`__djogi_test_inner_<N>`, `__djogi_apps_invocation_sentinel`, etc.). Adopters must not emit `__djogi_*` from `#[model]` definitions, custom column names, window aliases, relation macro names, or any other user-supplied identifier surface. Enforcement is centralized through `check_user_supplied_ident` / `assert_user_supplied_ident` for runtime caller-supplied names, `const_assert_user_supplied_ident` for const/inventory macro-support paths, and the macro-time validator for `#[model]` table/field names; `check_plain_ident` remains available for framework-derived internal identifiers that legitimately live in the namespace. `assert_no_alias_collision()` (the grouped-fetch SELECT-list duplicate-alias detector at `djogi/src/query/sql.rs`) remains a defensive collision check when a future SELECT path emits duplicate aliases. The set of names within the namespace is internal (no semver implications for adding/removing/renaming a specific `__djogi_*` identifier); the reservation rule itself is part of the public contract. **Framework-owned database namespace:** `djogi` is reserved for framework-owned DDL objects, and `djogi.__djogi_*` identifiers are framework-owned and not user-extensible; adopters cannot rely on these names for application schemas, generated SQL, or migration content. **Numeric array helper contract (Phase 8.5 G0):** `djogi.__djogi_numeric_array_is_rust_decimal_v1` is the canonical version-1 helper symbol (future semantic changes must use `..._v2`, etc.), and full helper-version migration paths are deferred to a future phase. **This phase:** compose must emit schema-qualified helper DDL once per migration file that references the helper, and `SchemaSnapshot` / `MigrationSnapshot` follow-up tracking for helper dependencies is tracked as a follow-up issue (not addressed in G0). **Temporal array helper contract (Phase 8.5 G0, post-ALLOW amendment):** `djogi.__djogi_date_array_is_finite_v1` and `djogi.__djogi_tstz_array_is_finite_v1` are the canonical version-1 helper symbols for `DATE[]` and `TIMESTAMPTZ[]` element-level representability CHECKs. These helpers apply `pg_catalog.isfinite(elem)` per unnested element so both `+infinity` and `-infinity` array elements are rejected — the prior `upper_bound >= ALL(col)` strategy admitted `-infinity` because `-infinity < upper_bound` (making the `>=` comparison TRUE in Postgres ordering). Same version-suffix rule (`..._v2` for future semantic changes) and same `SchemaSnapshot` tracking deferral as the numeric array helper. Full inventory + per-surface enforcement table in [`docs/spec/reserved-identifiers.md`](./reserved-identifiers.md). **Lens:** simplicity — one prefix to remember, one rule to enforce; production stability — framework can evolve internal SQL emission without coordinating with adopter code; honesty — framework-owned helper/version ownership and follow-up tracking scope are explicit in this row. |
| Djogi-as-dylib for shell + lihaaf (Phase 9) | 2026-05-10: djogi is buildable as a Rust dynamic library (`libdjogi.so`) and the Phase 9 shell binary dlopens it at startup; the lihaaf test harness shares the same coupling for its own reasons (see [`docs/spec/lihaaf-v0.1.md`](./lihaaf-v0.1.md), TBD). Rationale, in order of weight: (1) **Build iteration speed** — the shell crate (and lihaaf) iterates without re-linking djogi statically on every change, which collapses the inner loop for shell-side and harness-side work. (2) **Plugin ecosystem via `rhai-dylib`** — precompiled Rhai modules can ship as `.so` files that link against the canonical `libdjogi.so` rather than statically baking djogi into each plugin; as the module ecosystem grows this is the difference between linear and constant memory growth. (3) **Distribution size** — a ~5 MB shell binary plus a swappable ~30 MB `libdjogi.so` packages and ships better than a ~100 MB monolithic binary. (4) **Hot-reload potential** — speculative, not v0; rebuilding the dylib and dlopen-ing the new version into a running shell session is technically possible and the architecture leaves the door open. **Explicit non-goal:** dylib does NOT speed up runtime query execution. PLT/GOT indirection adds ~1-2 ns per cross-dylib call, lost in the noise next to the cheapest SQL round-trip. Anyone who frames dylib as a "faster queries" win is wrong; the speed wins are entirely on the build, plugin, memory, and distribution axes. **Spike-gated:** the inventory-on-dylib spike (see `docs/research/2026-05-10-inventory-on-dylib-spike.md`, TBD) validates whether `cargo rustc --crate-type=dylib` works for djogi AND whether `inventory::submit!` registrations propagate across the dylib boundary. Three contingencies named in `docs/spec/shell.md` §13.13 cover the fallback paths (`GO_WITH_MANIFEST` adds `crate-type = ["lib", "dylib"]` to djogi's `[lib]`; `GO_WITH_WORKAROUND` exposes explicit `lihaaf_inventory_collect_<T>()` per-collection re-exports; `NO_GO` ships Phase 9 statically-linked and defers the dylib-dependent benefits until the toolchain blocker resolves). The lihaaf harness consumes the same dylib but has no Rhai dependency — the dylib pattern is shared, the harnesses are independent. **Lens:** scalability — plugin ecosystem can grow without per-plugin memory cost; production stability — distribution surface stays bounded as djogi grows; honesty — the rationale names what dylib actually buys (build, plugin, memory, distribution) and explicitly disclaims the easy-but-wrong "faster execution" framing. |
| Decimal precision and scale projection (djogi#188, Phase 8.5 v3 Cluster 2) | `rust_decimal::Decimal` columns project as bare `NUMERIC` (unbounded) with a **structural CHECK** enforcing the Rust type's **exact representable** range: `scale(col) <= 28 AND abs(col) * power(10::numeric, scale(col)) <= 79228162514264337593543950335`. The column type stays bare `NUMERIC` so adopter writes keep their full precision and scale (no rounding); the CHECK rejects only values that fall outside rust_decimal's exact-representable domain — i.e., values that would either (a) be silently rescaled and rounded to scale 28 on decode (the pinned `rust_decimal::postgres::common::checked_from_postgres` calls `result.rescale((scale as u32).min(Self::MAX_SCALE))`, which loses precision rather than failing), or (b) overflow the 96-bit coefficient envelope and fail to decode outright (`checked_from_postgres` returns `None`, surfaced as `DjogiError::Decode`). Without the CHECK, an external writer could land a row with scale 50 that decodes to a rounded value — a silent precision loss the typed Rust path cannot detect. The CHECK closes the same external-writer hole `time::Date` carries: reject at the DB layer rather than let a malformed value silently lose precision or poison subsequent reads. The umbrella issue's literal recommendation (`Decimal → NUMERIC(28, 28)`) is mathematically incorrect: `NUMERIC(28, 28)` in Postgres means 28 total significant digits with 28 of them as scale (i.e., **0 integer digits**), so values with any integer component — including `49.99` in `tests/integration/phase1_model.rs::Product::price` — would not fit and Postgres would reject the column type. Alternative fixed shapes (`NUMERIC(28, 14)` balanced, `NUMERIC(28, 8)` crypto-satoshi, `NUMERIC(28, 6)` currency, `NUMERIC(38, 19)` SQL-Server-like) each silently round adopter writes — worse than rejecting them — and would require an adopter override attribute to escape that surprise. The structural-CHECK approach gives adopters the unconstrained scale Postgres natively offers while still rejecting out-of-rust_decimal-range writes. **Override story:** adopters who need a different shape today use `#[field(check = "<custom predicate>")]` (djogi#105, same PR) which combines with the type-derived CHECK via logical `AND`; the framework does not ship a `#[field(numeric_precision = N, numeric_scale = M)]` attribute yet (deferred — adopters with sustained currency-precision needs surface that work as an issue when they have a real use case). **Performance:** the CHECK runs one `power(10, scale(col))` per write (Postgres NUMERIC arithmetic + `scale()` function). The umbrella issue #185 records a per-write budget target for the type-derived CHECK family but remains open; a measured-µs claim against that budget is left to #185's benchmark workstream rather than asserted here. NULL values satisfy the CHECK by Postgres convention (CHECK treats UNKNOWN as satisfied). **Lens:** correctness — the CHECK rejects writes that would either silently lose precision (scale > 28 → rescale/round) or hard-fail to decode (coefficient > 2^96 - 1) on the typed Rust path; production stability — bare `NUMERIC` preserves adopter precision and scale verbatim, no silent rounding; scalability — projection runs once per descriptor compile, not per row; honesty — the rust_decimal exact-representability bound is the only DB-level invariant the framework can claim without making a guess about adopter precision needs. |
| Type-derived CHECK projection (Phase 8.5 v3 Cluster 2) | Columns whose Rust source type widens to a Postgres column type carry an automatic CHECK constraint bounding the column to the Rust source type's representable range. The projection is type-driven, lives at `djogi/src/migrate/projection.rs::field_type_check`, and runs at descriptor → snapshot lowering time so the resulting CHECK serializes into `schema_snapshot.json` and survives every round-trip. **Currently shipped under djogi#186 (contract layer)**: the `FieldSqlType::NumericPrecision { precision, scale }` variant (descriptor.rs), the `field_type_check` projection helper (projection.rs), the AMEND DROP+ADD lifecycle fix in the differ (diff.rs), and `IntoFilterValue for u64` (query/field.rs). **Currently shipped under djogi#187 (temporal family wiring)**: the projection wiring at `project_column` is live for non-FK columns, `field_type_check` carries `Date` → `<col> <= DATE '9999-12-31'` and `Timestamptz` → `<col> <= TIMESTAMPTZ '9999-12-31 23:59:59.999999+00'` arms emitting one-sided upper-bound CHECKs (the lower bound is omitted because Postgres's own date input parser rejects every value `time::Date` cannot represent on the lower end — Postgres's MIN is 4713 BC, `time::Date`'s MIN is 10000 BC, so values in ISO years -9999 to -4713 are physically unreachable through Postgres regardless of CHECK), and the SQL emitter renders inline CHECKs with explicit `CONSTRAINT <name> CHECK (...)` keywords so the constraint name is deterministic (Postgres's auto-naming for unnamed inline CHECKs uses `{table}_check` / `{table}_check1` — inconsistent with the differ's ALTER TABLE DROP CONSTRAINT path). The temporal arms close the bug class for `time::Date` / `time::OffsetDateTime` columns at the DB layer. **Currently shipped under djogi#188 (Decimal family wiring)**: adopter `rust_decimal::Decimal` fields carry `Some(RustSourceType::Decimal)` and project a structural CHECK enforcing the 96-bit mantissa / scale-≤-28 representable range; the column stays bare `NUMERIC` (no precision/scale on the column) so adopter writes preserve their full precision. The `Decimal` discriminator does NOT trigger a bind/decode shim — `rust_decimal::Decimal` already implements `postgres_types::ToSql for NUMERIC` natively. **Currently shipped under djogi#190 (integer family wiring)**: `FieldDescriptor` carries `rust_source_type: Option<RustSourceType>`; per-field bind/decode shims bridge the `postgres_types::ToSql` gap (`i8` binds via `i16::from(v)`, `u32` via `i64::from(v)`, `u64` via `Decimal::from(v)`). The integer arms inside `field_type_check` dispatch on the discriminator: `SmallInt` + `RustSourceType::I8` → `-128..=127` CHECK; `SmallInt` + `RustSourceType::U8` → `0..=255`; `Integer` + `RustSourceType::U16` → `0..=65535`; `BigInt` + `RustSourceType::U32` → `0..=4294967295`; bare `Numeric` + `RustSourceType::U64` → `0..=18446744073709551615 AND col = trunc(col)` (the integrality clause is necessary because bare `NUMERIC` admits fractional inputs without a constraint). `u64` uses bare `Numeric` — not `NUMERIC(20, 0)` — because `NUMERIC(20, 0)` rounds on write whereas bare `NUMERIC` with `col = trunc(col)` rejects fractional inputs outright. Direct-mapped Rust types (`i16 → SMALLINT`, `i32 → INTEGER`, `i64 → BIGINT`) carry no `RustSourceType` discriminator and return `None` from `field_type_check`. Identity-mapped widths (`i16 → SMALLINT`, `i32 → INTEGER`, `i64 → BIGINT`, `bool / String / f32 / f64 / ...`) project no CHECK because the column type already covers their full range and a redundant CHECK would inflate every snapshot. FK columns inherit the parent PK's identity-width type, so they project no Rust-derived CHECK either (adopter `#[field(check)]` on an FK column is still honoured — see the "Adopter `#[field(check)]`" row below). The type-derived CHECK contract is family-extensible: HeerId / RanjId structural validation (djogi#189) plugs into the same `field_type_check` match without reshaping the helper signature. **`f32 → REAL` lock (djogi#185)**: `f32` is locked at the identity-mapped row — widening to `DOUBLE PRECISION` would silently corrupt values exceeding `f32::MAX` on the typed `SELECT` path through `row.try_get::<f32>` (the wire `DOUBLE PRECISION` value loses precision and may overflow `f32::INFINITY`). This is a permanent design rule, not an oversight. Adopters who need wider floats must declare `f64` columns explicitly. **Lens:** correctness — the temporal CHECK closes the external-writer hole the typed `SELECT` path silently exposed for `time::Date` / `time::OffsetDateTime`, the integer family wiring (djogi#190) constrains each narrow Rust integer type to its representable range while preserving no-CHECK for identity-mapped types, and the Decimal family wiring (djogi#188) rejects writes that fall outside rust_decimal's exact-representable domain — both the silent rescale-and-round case (scale > 28) and the hard decode-failure case (coefficient > 2^96 - 1) — at the DB layer; production stability — the AMEND DROP+ADD lifecycle ensures CHECK expression changes never collide on the constraint name slot, the explicit `CONSTRAINT <name>` keyword on inline CHECK emission keeps CREATE TABLE and ALTER TABLE pathways converged on a single constraint slot, and (post GPT-5.5 review) the `ColumnChange::SetCheck { from, to }` IR carries the prior CHECK so rollback restores it losslessly even for type-changed checked columns; scalability — projection runs once per descriptor compile, not per row. |
| Adopter `#[field(check = "<sql>")]` (djogi#105, Phase 8.5 v3 Cluster 2) | Adopters declare arbitrary CHECK constraints on any model field via `#[field(check = "<sql expression>")]`. The expression is emitted verbatim into the column's CHECK constraint inside both inline `CREATE TABLE` form (`CONSTRAINT <table>_<column>_check CHECK (<expr>)`) and the differ's ALTER-TABLE form (`ALTER TABLE … ADD CONSTRAINT … CHECK (<expr>)`). The attribute is treated as a **raw SQL escape** — djogi performs no parsing, sanitization, or semantic validation beyond rejecting empty / whitespace-only literals at parse time. Adopters carry the same `unsafe`-style cultural responsibility documented in `docs/spec/raw-sql-escape-hatches.md`: every callsite is reviewable as raw SQL, syntactic correctness and `IMMUTABLE`-volatility compliance are the adopter's responsibility, and identifier handling (reserved-word column references, quoting) is the adopter's responsibility. **AND-merge with type-derived CHECK.** When a column carries both an adopter `#[field(check)]` and a type-derived CHECK (e.g. a `u32` field with `#[field(check = "port > 0")]`), the projection layer combines them via logical `AND` into a single constraint slot (`<table>_<column>_check`): `CHECK ((<type-derived>) AND (<adopter>))`. Both clauses must pass for an INSERT / UPDATE to land. The single constraint slot keeps the ADD / DROP / AMEND lifecycle in the differ unchanged — every column has at most one CHECK at `<table>_<column>_check`, regardless of whether it came from the framework, the adopter, or both. **FK columns.** Adopter CHECKs on FK columns are honoured even though the type-derived CHECK is suppressed (FK column types inherit from the parent's PK, always identity-width). The adopter may want a domain invariant on the FK column itself — e.g. `#[field(check = "owner_id > 0")]` to reject the HeerId sentinel zero on a non-null FK column. **Validation.** `FieldAttrs::parse` rejects empty and whitespace-only literals with a span-precise diagnostic. SQL-level rejection (syntax errors, unknown column references) happens at migration apply time; Postgres returns the full constraint name in the error message, making the field clearly identifiable. **Lens:** correctness — adopter CHECK violations surface at the DB layer before bad rows land, with deterministic constraint-name reporting; production stability — the AND-merge preserves the single-constraint-slot invariant the differ depends on; honesty — the "raw SQL" framing is explicit, mirroring how the framework treats `raw_*` access; idiomatic Rust — the attribute syntax matches the existing `#[field(default = "...")]` / `#[field(generated = "...")]` family precisely. |
| Visage-derived fields (djogi#231, Phase 8.5) | Adopters declare struct-level `#[derived(name, ty, scopes, sql, rust, doc)]` attributes on `#[model]` structs to surface a projection entry that does not correspond to a storage column. The derived value is computed two ways: a Postgres SQL expression at fetch time (server-side, rendered into the visage's SELECT `PROJECTION_LIST` as `(<sql>) AS <alias>`) and a Rust expression at in-memory construction time (evaluated inside the visage's `From<&Model>` / `TryFrom<&Model>` body with a `let model: &{Model} = src;` rebind). The framework does NOT translate between the two; adopters write both sides and the [`djogi::testing::DerivedParity`] helper catches drift. **Design choices.** (1) **Struct-level `#[derived]` over field-level `#[computed(... expose)]`.** The earlier ten-round Path A draft declared derived fields by extending `#[computed]` with an `expose(...)` sub-attribute on a model field — treating the derived value as a virtual model field. Path B reframes visages as first-class projections, not narrowed models with optional add-ons; derived projections belong at the projection definition site (the visage, addressed by `scopes = [...]`), not as a phantom field on the source model. The model struct stays pure storage. (2) **Syntactic-tail fallibility detection — no explicit `fallible = true` key.** The macro recognises a closed set of expression shapes as fallible (trailing `?`, `Ok(...)` tail, `Err(...)` tail, `match { Ok(...) / Err(...) }` tail). If any derived entry is fallible, the whole visage lifts to `TryFrom<&Model, Error = VisageError>`; infallible entries pass through the `From<Infallible> for VisageError` glue. The expression itself is the source of truth — an explicit key would be redundant and could disagree with the actual shape. (3) **Lowercase-only `name`.** Postgres folds unquoted identifiers to lowercase server-side; a `name = FacilitySite` would silently rename to `facilitysite` on the wire and break positional decode. Rejecting uppercase bytes at parse time (E_DJG_VDF_012) is the smaller surface than double-quoting every alias. (4) **Sealed `ProjectionEntry` under `__private`.** The trait constant `DjogiVisage::PROJECTIONS: &'static [ProjectionEntry]` needs the enum to be `pub` (the type appears in a `pub` trait constant); routing it through `::djogi::__private::ProjectionEntry` plus `#[doc(hidden)]` plus `#[non_exhaustive]` plus the "do not construct" convention warning matches the existing `__private::VisageSealed` / `__private::pk_seal` precedent. The warning is the seal at the language-of-conduct level; the `#[non_exhaustive]` attribute prevents exhaustive matching across the crate boundary. (5) **Descriptor inventory separated from storage/migration inventory.** Derived projection metadata lands in a dedicated [`djogi::descriptor::VisageDescriptor`] inventory channel (one descriptor per `(Model, scope)` pair with derived entries in scope), collected via `inventory::collect!(VisageDescriptor)` — structurally separate from the [`ModelDescriptor`] / [`EnumDescriptor`] collections the migration differ walks. The migration / snapshot / `build.rs` paths never observe `VisageDescriptor` entries, preserving the storage-vs-projection split. `DerivedProjection` records carry the richer shape (`ty_path` token-string, `rust` source, `doc`, originating `scopes`) that documentation generators and future Tier-2 predicate renderers need without bloating the per-visage trait constant or the SELECT-emission hot path. **Lens:** correctness — paired SQL/Rust expressions stay verifiable through the parity helper rather than via a closed SQL grammar the macro would need to embed (incompatible with the no-regex discipline and the no-third-party-query-builder rule); scalability — descriptor inventory is `&'static`-only so `inventory::submit!` ships zero per-model runtime allocation; production stability — separate inventory channels guarantee derived projections never leak into the migration differ's storage-side reflection; honesty — the syntactic-tail detection rule is a closed, testable surface that the adopter can predict without consulting the macro's source. |
| <a id="aggregate-annotation-declaration-site"></a>Aggregate annotation declaration site (djogi#228, Phase 8.5 design-lockdown) | Aggregate and window-function projections are **not** model-field attributes. The `#[derived]` Path B reshape (djogi#231 / [`visage-derived-fields.md`](./visage-derived-fields.md)) located projection-only entries at the visage struct level precisely because bundling storage + audience concerns at the model-field level (the prior `#[computed(sql, expose)]` shape) conflated two different things. The future aggregate / window-function surface inherits that lesson by construction: declaration sites are restricted to one of two shapes. **Shape Q (QuerySet-side).** A typed `.annotate(...)` terminal on `QuerySet<T>` / `VisageQuerySet<V>` composes per-query group-by aggregates and window expressions, producing a queryset return type that carries the aggregate columns alongside the base projection. No model-field attribute; no model-side declaration. Phase 6.5 already shipped the grouped/windowed substrate (`GroupedQuerySet` / `GroupedAnnotatedQuerySet`, `AggregateExpr::over`, `__djogi_agg_N` aliasing) which Shape Q extends from anonymous-aggregate use into named per-query projections. **Shape V (Visage-side, via `#[derived(..., aggregate = true)]`).** Per-row aggregates — window expressions and correlated-subquery scalars whose semantics produce one value per visage row — route through the existing `#[derived(...)]` helper attribute with an explicit `aggregate = true` marker. This marker is the deliberate opt-in that relaxes `#[derived]` Tier 1's [E_DJG_VDF_009](./visage-derived-fields.md#error-taxonomy) aggregate rejection; without it, aggregates inside `#[derived]` `sql` continue to surface as a parse-time error. The marker exists on the struct-level `#[derived]` attribute (visage-scope address), never on a model field. **Shape hybrid.** Group-by aggregates (which inherently collapse rows) take Shape Q; per-row aggregates (window expressions, correlated subqueries) take Shape V. Two surfaces for two semantically distinct shapes; one declaration-site rule for both: never the model field. **What is locked now, independent of Shape Q / Shape V / hybrid choice.** (1) No `#[annotation(sql, expose)]` field-level attribute. (2) Visage-side aggregates route through `#[derived]` with the explicit `aggregate = true` marker, not through a new attribute namespace that re-opens the conflation. (3) Queryset-side aggregates live on `QuerySet` / `VisageQuerySet`, not on the model declaration. (4) Tier 1's pre-emptive `E_DJG_VDF_009` aggregate rejection remains correct for `#[derived]` without `aggregate = true`; only the explicit marker opt-in relaxes it. (5) Tier 2 / Tier 3 filter and order-by on derived fields ([`visage-derived-fields.md` §Capability tiers](./visage-derived-fields.md#capability-tiers)) consume any aggregate that lands through Shape V on equal footing with scalar derived fields. **Why lock pre-implementation.** The visage-derived-fields Path A draft took ten review rounds before the Shape A model-field conflation was caught and reshaped. The cost of pinning the aggregate declaration-site rule before any implementation pressure is ~zero (this row); the cost of re-catching it during implementation review is multiple rounds of rework — the same trajectory `#[computed(sql, expose)]` produced. **Where the implementation spec lands.** A future `docs/spec/aggregate-annotations.md` is created when implementation is scheduled (no earlier), and it amends this row's three rules in place rather than supplanting them. **Lens:** correctness — declaration site honours the storage-vs-projection boundary the `#[derived]` reshape paid for; idiomatic Rust — Shape Q reuses the existing closure-typed queryset surface, Shape V reuses the existing `#[derived]` helper attribute; simplicity — one rule for both shapes (declaration site is never the model field); honesty — the lock-in cites the prior conflation it learns from, rather than presenting itself as a fresh insight. |
| <a id="aggregate-kind-state-within-group"></a>Aggregate kind-state and `WITHIN GROUP` aggregates (djogi#88, Phase 8.5) | `AggregateExpr<Out, K = ValueAgg>` carries a sealed aggregate-family kind marker so illegal modifier combinations are rejected by Rust method lookup instead of deferred to SQL construction. The blessed public markers are `ValueAgg`, `MetadataAgg`, `OrderedSetAgg`, and `HypotheticalSetAgg`; the sealing trait `KindEvidence` is implemented only by Djogi, preventing downstream crates from fabricating a kind that bypasses the modifier discipline. `ValueAgg` is the default so existing `AggregateExpr<Out>` spellings remain source-compatible and expose the ordinary value-aggregate modifiers (`distinct`, `filter`, per-aggregate `order_by`, and `over`). `MetadataAgg` is used for `GROUPING` and exposes no modifiers. `OrderedSetAgg` (`percentile_cont`, `percentile_disc`, `mode`) and `HypotheticalSetAgg` (`rank_of`, `dense_rank_of`, `percent_rank_of`, `cume_dist_of`) expose only `filter` and `within_group_order_by`. The first supported `WITHIN GROUP` slice is deliberately single-column: the receiver field eagerly populates `WITHIN GROUP (ORDER BY receiver ASC)`, and `.within_group_order_by(...)` replaces that target when a different single order key is needed; the replacement target must preserve the aggregate's type contract — for ordered-set aggregates (`percentile_cont`, `percentile_disc`, `mode`) the replacement column must be the same SQL/Rust decode type as the receiver column so the aggregate's return type remains valid, and for hypothetical-set aggregates (`rank_of`, `dense_rank_of`, `percent_rank_of`, `cume_dist_of`) the replacement column must be comparable to the supplied argument value's type. Multi-column tuple arguments and multi-key hypothetical-set rows are deferred until the tuple argument design is scheduled. **Lens:** correctness — invalid aggregate/modifier combinations fail before query execution; simplicity — eager default `WITHIN GROUP` avoids an extra ready/not-ready typestate parameter while preserving the mandatory-order invariant for the public constructors; production stability — the sealed kind set gives Djogi room to add future aggregate families without exposing an unsafe external extension hook. |
| Visage exposure default — per-field opt-in, no struct-level default (djogi#229 surface 2, Phase 8.5 design-lockdown) | Per-field `#[field(expose(...))]` is the **only** declaration site for visage exposure; no struct-level `#[model(visage_default = [...])]` (or equivalent) default exists or will be added. Absent annotation maps to `internal` — the field appears in **none** of the four generated visages (`{Model}Public` / `SelfView` / `Admin` / `Export`). The default is enforced in `djogi-macros/src/model/visage_ctx.rs::classify_field_for_scope`, where `(scalar_hit=false, relation_hit=None, _)` returns `ScopeMembership::Absent`. **Why per-field, not struct-level.** The umbrella analysis (djogi#229 surface 2) raised the question of whether adding a new audience scope to a model creates unnecessary friction — each existing field must be touched to add (or omit) the new scope's name. A struct-level default would let new fields inherit a default visage set, reducing per-field churn. The decision rejects that direction: per-field declaration is a **security-by-default** invariant, not a friction tax. A struct-level default inverts the safe direction — a developer adding a new sensitive field (`password_hash`, `api_key`, internal-only flags, business-secret pricing) would have to remember to opt **out** (`expose(none)` or omit from default) rather than opt **in**, and forgetting opt-out leaks data into transport visages by silent inheritance. The current per-field rule means every transport-shape inclusion is an explicit reviewable token at the field declaration site, the same way `pub` is per-item in Rust and `#[serde(rename = "...")]` is per-field in serde. **Alternatives that are NOT this.** Struct-level **renaming** transforms (e.g., `serde(rename_all = "camelCase")`) are a separate concern — they transform a field's *existing* visibility into a serialisation shape, not the visibility itself. The analogy to camelCase does not extend to exposure. Per-app or per-scope `#[scope_default]` macros that *expand* into per-field annotations at parse time are likewise out of scope: any sugar that hides per-field decisions defeats the security-by-default invariant. **What this row does NOT lock.** Future work to streamline declarations of *uniform* exposure across many fields (e.g., a `pub use djogi::expose_all_public_admin_export` macro that adopters apply per-field) remains open — but the declaration must remain per-field and reviewable at each field site. **Lens:** correctness — the per-field rule means a new sensitive field cannot silently leak via inheritance; production stability — auditing a `#[model]` definition for transport exposure requires only reading the field declarations, not joining against a struct-level default; idiomatic Rust — matches `pub`'s per-item discipline and serde's per-field rename pattern; honesty — declaration friction is real, but the security-by-default direction is the framework invariant the lower-severity adopter-friction story does not outweigh. |
| Model-vs-visage adjacent-pattern graduation (djogi#229, Phase 8.5 design-lockdown) | Seven lower-severity surfaces from the post-`#[derived]`-reshape model-vs-visage pattern hunt have been graduated. The full per-surface analysis lives at [`docs/research/model-vs-visage-lower-severity-graduation.md`](../research/model-vs-visage-lower-severity-graduation.md). **Summary of outcomes.** Surface 1 (`#[field(generated)]` per-scope projection) is `phantom` — the mechanism exists today via ordinary `#[field(expose(...))]` annotations; the umbrella's premise was incorrect, verified against `djogi-macros/src/model/visage_ctx.rs::classify_field_for_scope`. Surface 2 (per-field `expose` vs struct default) is `accepted-design` and locked by the row above. Surface 3 (relation-form embedding declaration site) is `accepted-design (conditional)` — visage names are mechanically `{Model}{Scope}` today (renaming is in `docs/spec/visages.md` Deferred Surface), so the umbrella's "rename forces touching every embedder" scenario is not possible until the deferred custom-visage-names feature ships; that future spec carries the explicit obligation to address embedding-site churn. Surface 4 (`proxy_for + default_filter / default_order`) **shipped in Phase 8β (v0.1.0)** — the bundled `#[model(proxy_for, default_filter, default_order)]` shape is the accepted shipped design (parser at `djogi-macros/src/model/attrs.rs`, guide at `docs/guide/proxy.md`); the Shape A concern (identity bundled with queryset-level query defaults) is reclassified as a **post-shipment design question** requiring a migration story before any reshape lands. Surface 5 (`{Model}_logs` per-audience access) is **re-categorised** out of the visage domain into access-control / RBAC, anchored at Phase 5.5 / Phase 10 (Maahi); the proper issue lives in the access-control namespace, not the visage namespace, to avoid re-introducing the Shape A conflation. Surfaces 6 (M2M through-model visage suppression) and 7 (tree-queries-into-visage projection) earn **deferable standalone issues** — low-priority ergonomic / queryset items where adopter feedback after v0.1.0 publish is the right driver; proposed titles are recorded in the research note. **What this row locks now.** (1) The two design rules above (this row and the previous row) are the only pre-emptive locks earned by the umbrella's per-surface analysis. (2) Future contributors must read the research note before re-opening any of the seven surface questions, so that the verification chain (Did anyone reproduce the claim against current code? What did the user-lens prioritisation conclude?) remains visible. (3) The proposed-issue titles in the research note are recommendations, not commitments — the orchestrator files them when the work is taken up; this row authorises the recommendation but not the auto-file. **Lens:** correctness — every surface decision was verified against current code rather than carrying forward the umbrella's claims unchallenged; production stability — surface 4 shipped before this analysis ran (Phase 8β) and its Shape A concern is reclassified as a post-shipment design question; surface 5 re-categorisation work would surface as multi-round rework if access-control tooling proceeded under the visage-namespace framing; idiomatic Rust — the per-surface rationales preserve the existing patterns (typed closures on QuerySet, helper attributes on `#[derive(Model)]`, trait impls for model-bound logic) rather than introducing new attribute namespaces per surface; simplicity — three of seven surfaces resolve to "no action needed" (phantom, accepted-design, accepted-design-conditional), shrinking the issue surface before the v0.1.0 publish gate. |
| INTERVAL typed surface (djogi#212, Phase 8.5 Cluster 4) | Postgres `INTERVAL` columns ship as the typed Rust newtype `djogi::Interval { months: i32, days: i32, microseconds: i64 }` with a hand-rolled `postgres_types::{ToSql, FromSql}` impl against the 16-byte Postgres binary wire format (`microseconds: i64 BE`, `days: i32 BE`, `months: i32 BE`). The descriptor variant is `FieldSqlType::Interval`; the macro maps `Interval` (bare, after `use djogi::prelude::*;`), `djogi::Interval`, `djogi::types::Interval`, and `::djogi::types::Interval` (absolute form — the leading `::` is stripped before matching, so absolute paths converge on the same arms as their relative counterparts) to `FieldSqlType::Interval` in `rust_type_to_sql`. Bare `Interval` is recognised because adopters reach the type through the prelude — the same way `Date`, `DateTime`, `HeerId`, and `RanjId` are recognised in bare form from the same prelude. **Why three components, not `time::Duration`.** Postgres `INTERVAL` is a tagged three-tuple: `2 days` is not `48 hours` (DST shifts), and `1 month` has no fixed microsecond count (28-31 days depending on anchor). Collapsing the three into a `time::Duration` would silently corrupt these semantics. The newtype mirrors the wire format exactly. **No regex / no third-party crate.** The wire codec is byte-level `i64::from_be_bytes` / `i32::from_be_bytes` against the 16-byte payload — no `pg_interval` crate dependency. **Rust structural equality vs Postgres SQL `=`.** `Interval` derives `PartialEq` / `Eq` / `Hash` structurally — all three components must match byte-for-byte. `Interval::months_only(1) != Interval::days_only(30)` in Rust. `Hash` mirrors `PartialEq` by Rust convention, so Rust-side hashmap keying is structural, never linearized. Postgres `=` on an `INTERVAL` column linearizes: months are treated as 30 days, days as 24 hours (86,400,000,000 µs); `INTERVAL '1 month' = INTERVAL '30 days'` is `true` in Postgres SQL. `QuerySet::filter(|f| f.duration().eq(...))` forwards to Postgres `=` and therefore follows Postgres linearization semantics, not Rust structural `==` — `.eq(Interval::months_only(1))` will also match rows storing `Interval::days_only(30)`. This deliberate divergence is pinned by `interval_sql_eq_linearizes_months_and_days` in `tests/integration/phase8_5_c4_212_interval.rs`. **Lens:** correctness — calendar semantics preserved verbatim against the Postgres model; production stability — wire-format match is deterministic and frozen by Postgres's binary protocol; honesty — the Rust structural semantics vs. Postgres linearization is called out explicitly so adopters don't conflate them, and the three-field struct surfaces the semantic distinction adopters need to reason about; idiomatic Rust — `Default + PartialEq + Hash` derived; `Ord` deliberately omitted because cross-component ordering is ambiguous without a comparison anchor. |
| `Range<T>` typed substrate (djogi#148 + djogi#150, Phase 8.5 G0) | Postgres built-in range columns ship as the conservative typed Rust value `djogi::Range<T>` with `RangeBound::{Inclusive, Exclusive, Unbounded}` plus an explicit empty-range sentinel. G0 supports exactly `Range<i32>` -> `int4range`, `Range<i64>` -> `int8range`, `Range<rust_decimal::Decimal>` -> `numrange`, `Range<DateTime>` -> `tstzrange`, and `Range<Date>` -> `daterange`; `tsrange` is intentionally omitted because Djogi temporal columns use timezone-aware `TIMESTAMPTZ`. The descriptor surface is `FieldSqlType::Range { subtype: RangeSubtypeKind }`, and macro lowering recognises bare `Range<T>`, `djogi::Range<T>`, and `djogi::types::Range<T>` path forms. G0 now routes scalar representability checks through range endpoints (`lower(col)` + `upper(col)`) so `NUMRANGE` and `TSTZRANGE`/`DATERANGE` columns reuse element semantics with `IS NULL` guards, preserving NULL/unbounded/empty/explicit NULL pass-through; `INT4RANGE` and `INT8RANGE` stay no-op. This row is substrate-only: #148 owns future `btree_gist` EXCLUDE grammar, `&&` overlap operators, and extension migration behavior; #150 owns future PostgreSQL 18 `WITHOUT OVERLAPS`, `PERIOD` FK, `NOT ENFORCED`, and named `NOT NULL` DDL. **Lens:** correctness - one shared range terminology and element-check reuse keeps no-overlap lanes aligned; production stability - wire codecs route through the element type Postgres binary codec and reject malformed NULL-bound/trailing-byte payloads; honesty - Rust structural equality is not Postgres range canonicalization, so DB-level no-overlap semantics remain future lane work; simplicity - no third-party range adapter dependency. |
